class Solution {
    public int minOperations(int[] nums, int k) {
        int sumOfArrayElements = 0;
        for(int i = 0 ; i < nums.length; i++){
            sumOfArrayElements += nums[i];
            }
        return sumOfArrayElements%k;
        
    }
}
